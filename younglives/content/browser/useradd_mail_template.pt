<tal:root define="lt string:&lt;;gt string:&gt;;
                  dummy python:request.RESPONSE.setHeader('Content-Type', 'text/plain;; charset=%s' % context.plone_utils.getSiteEncoding());
                  portal python:options['portal'];
                  member_id python:options['member_id'];
                  member_username python:options['member_username'];
                  member_fullname python:options['member_fullname'];
                  reset python:options.get('reset', None) or portal.portal_password_reset.requestReset(member_id);"
>
Welcome <span tal:replace="member_fullname" />,

your user account has been created. Please activate it by visiting

<span tal:replace="python:portal.pwreset_constructURL(reset['randomstring'])+'?userid='+member_username" />

You must activate your account within <span tal:replace="portal/portal_password_reset/getExpirationTimeout" />
hours, so before <span tal:replace="python:context.toLocalizedTime(reset['expires'], long_format=1)" />

With kind regards,
--
Site Administrator
</tal:root>